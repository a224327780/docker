FROM ghcr.io/a224327780/debian

WORKDIR /home

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update -y \
    && apt-get install -y --no-install-recommends cmake autoconf automake libtool nasm make pkg-config git zlib1g-dev 
RUN git clone https://github.com/mozilla/mozjpeg.git \
    && cd mozjpeg \
    && cmake -G"Unix Makefiles" -DPNG_SUPPORTED=OFF \
    && make install \
    && ln -sf /opt/mozjpeg/bin/cjpeg /usr/bin/mozjpeg

CMD ["mozjpeg", "--version"]