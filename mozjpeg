FROM debian:bullseye

ENV DEBIAN_FRONTEND=noninteractive TIME_ZONE=Asia/Shanghai

RUN apt-get update -y \
    && apt-get install --no-install-recommends cmake autoconf automake libtool nasm make pkg-config git zlib1g-dev -y\
    && git clone https://github.com/mozilla/mozjpeg.git \
    && cd mozjpeg \
    && cmake -G"Unix Makefiles" -DPNG_SUPPORTED=OFF \
    && make install \
    && ln -sf /opt/mozjpeg/bin/cjpeg /usr/bin/mozjpeg

CMD ["mozjpeg", "--version"]